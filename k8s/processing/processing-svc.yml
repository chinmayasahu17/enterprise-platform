apiVersion: v1
kind: Service
metadata:
  name: processing-service

spec:
  selector:
    app: processing

  ports:
    - name: http
      port: 8080
      targetPort: 8080

    - name: management
      port: 8081
      targetPort: 8081

  type: ClusterIP
